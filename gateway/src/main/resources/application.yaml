
spring:
  cloud:
    gateway:
      routes:
        - id: api
          uri: lb://api
          predicates:
            - Path=/api/**
      default-filters:
        - name: Retry
          args:
            retries: 3
            methods: GET, POST, PUT, DELETE
  application:
    name: gateway
